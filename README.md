# pwdgen
A primitive password generator in Rust

## Usage
Generate a password with `pwdgen <option> <length>`. If no option and length is supplied, a password with 16 chars and the "all" configuration will be generated.

There are four options: `upper`, `lower`, `numerical`, and `all`.
